import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title:
    "Teachify – All-in-One Institution, School & Tuition Management System",
  description:
    "Teachify is a powerful and scalable management system for schools, colleges, and tuition centers. Streamline operations with role-based dashboards, student & tutor management, fees, courses, enrollment, and more — all in one centralized platform.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
